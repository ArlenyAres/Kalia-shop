export function CheckoutPage() {
  return (
    <>
      <section className="page-hero">
        <span className="eyebrow">Checkout</span>
        <h1 className="section-title">Secure payment flow</h1>
        <p className="text-muted">
          Ready for Stripe Elements, PayPal buttons, shipping forms, and order confirmation.
        </p>
      </section>
      <section className="page-card">
        <p>The client dependencies already include form validation, payment SDKs, and toast feedback.</p>
      </section>
    </>
  );
}
