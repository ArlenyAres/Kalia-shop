export function CartPage() {
  return (
    <>
      <section className="page-hero">
        <span className="eyebrow">Cart</span>
        <h1 className="section-title">Your selected pieces</h1>
      </section>
      <section className="page-card">
        <p>Add cart context and persistence here. The route is already registered in the router.</p>
      </section>
    </>
  );
}
